# NovaByte Demo

Demo de un sistema e-commerce (tienda + panel de administración). Backend
en `back/` (FastAPI + PostgreSQL), frontend en `front/` (React + Vite).

Ver `back/README.md` y `front/README.md` para instrucciones de setup.
